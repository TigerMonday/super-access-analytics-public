# GTM実装手順書

> 対象: {{クライアント名}} | 作成日: {{YYYY-MM-DD}}

---

## 前提条件

- GTM コンテナ: {{container_id}}
- GA4 測定 ID: {{measurement_id}}
- 実装担当: {{implementer}}

---

## 変数設定

### 新規作成が必要な変数

| 変数名 | 変数タイプ | データレイヤー変数名 | 用途 |
|--------|---------|-----------------|------|
| {{var_name}} | {{var_type}} | {{dl_var}} | {{purpose}} |

### 設定手順

1. GTM コンソールで「変数」→「ユーザー定義変数」→「新規」をクリック
2. 変数タイプ: **データレイヤーの変数** を選択
3. データレイヤー変数名: `{{dl_var_name}}` を入力
4. 変数名: `{{var_name}}` で保存

---

## トリガー設定

### 新規作成が必要なトリガー

| トリガー名 | タイプ | 発火条件 |
|----------|------|---------|
| {{trigger_name}} | {{trigger_type}} | {{condition}} |

---

## タグ設定

### {{event_name}} タグ

**タグ名:** `[GA4] {{event_name}}`
**タグタイプ:** Google アナリティクス: GA4 イベント

| 設定項目 | 値 |
|---------|-----|
| 測定 ID | `{{measurement_id}}` |
| イベント名 | `{{event_name}}` |
| トリガー | `{{trigger_name}}` |

**イベントパラメータ:**

| パラメータ名 | 値 |
|-----------|-----|
| {{param_name}} | `{{{{var_name}}}}` |

---

## テスト観点チェックリスト

- [ ] プレビューモードで各タグが正しいトリガーで発火する
- [ ] GA4 DebugView でイベントとパラメータが受信されている
- [ ] パラメータの値が期待通りである（型・フォーマット）
- [ ] キーイベントが GA4 管理画面に登録されている
- [ ] 本番公開後 24 時間以内に GA4 でイベントが確認できる

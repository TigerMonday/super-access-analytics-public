"""GA4のどのプロパティにも既定で入り込むイベント名（判定対象にしないノイズ）.

`purchase` はGA4のキーイベント候補（推奨イベント）としてどのプロパティにも既定で
出てくる。ECを使っていないサイトでも「候補」のまま登録されていることが多く、これを
他の登録済みイベントと同列に「発火0件の成果」「使われていないイベント」として
指摘に出すと、案件を問わず毎回同じノイズになる。そのため判定対象から除外する
（ユーザー確認済みの運用判断）。

除外は購入計測そのものを否定するものではない。人が明示的にKPI・key_eventsに
`purchase` を登録した場合はその意思を尊重し、除外の対象にしない
（`kpi_coverage.check_coverage` の「events登録済みKPI」経路は本リストを見ない）。

audit_matrix / kpi_coverage / questions の3か所で同じ判断を使うため、ここに1つだけ置く。
"""

from __future__ import annotations

# ここに追加するときは、必ず「どのプロパティにも既定で入る」という同じ理由に限定する。
# 案件固有の事情で個別イベントを増やすと、判定漏れの温床になる。
DEFAULT_NOISE_KEY_EVENTS = frozenset({"purchase"})

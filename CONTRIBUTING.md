# コントリビューション

## 意見・質問を送る

コードが書けなくても参加できます。[会社のお問い合わせフォーム](https://sdjhe.share.hsforms.com/2YuKJnaAHSpW95m9Hqw5kqA)から、疑問やフィードバックをお寄せください。利用者からの受付は、公開Issueではなくこのフォームにまとめています。

- 不具合の報告：何をしたときに、期待と違う結果になったか。
- 機能・分析内容の改善要望：何に困っていて、どうなると助かるか。
- 使い方の相談・感想：迷った箇所や、使って役立った点。

クライアント名、実URL、GA4/GTM ID、実データ、第三者の個人情報、認証情報は送らないでください。返信先はフォームの連絡先欄に入力してください。ログは必要な箇所だけを架空の値に置き換え、レポートや設定ファイルを丸ごと送らないでください。脆弱性は [SECURITY.md](SECURITY.md) の非公開窓口へ報告してください。

管理者は内容を確認し、返信先がある場合は必要に応じて追加情報を聞きます。改善課題をIssueなどに公開する場合は、個人や会社を特定できる情報を除いて要約し、フォームの回答をそのまま転載しません。対応するものは優先順位を決めて修正・検証し、公開した版と変更内容を案内します。すべての要望の採用や、回答・修正の期限は約束していません。

## コードや手順を改善する

このリポジトリは閲覧・クローンによる利用のために公開しています。外部からのPull Request（PR）は受け付けていません。改善案や不具合は上記フォームからお寄せください。管理者が内容を確認し、必要な修正を行います。

以下は管理者が開発するときの手順です。

## 開発前に読むもの

1. `AGENTS.md`
2. `docs/ai-agent-guide.md`
3. `docs/standard-run-order.md`（運用ルールの正本）
4. 変更対象機能のREADME・spec・prompt

## データを持ち込まない

コード、手順書、テスト、サンプルへ、実在クライアント名、実URL、実GA4/GTM ID、認証情報、
実データの数値を書かないでください。テストは `example.com` と明示的な架空データを使います。
`outputs/` と `01_context_management/context/` はローカル専用で、コミットしません。

## 変更時の確認

- 変更したプロジェクトのテストを実行する。
- `uv run python tools/check_links.py` でリンクを確認する。
- コミット直前に、ほかのコマンドと連結せず `uv run python tools/leak_check.py` を実行し、結果を読む。
- 生成ファイルだけを直さず、正本・生成処理・テストを一緒に更新する。
- CVRは、確認済みCVイベントが発生したセッション数を分子にする。イベント回数を分子にしない。

## 変更の説明

変更記録には、課題点、修正内容、得られる効果、テスト結果、データ取り扱いへの影響を記載してください。

[project]
name = "adhoc-analysis"
version = "0.1.0"
description = "アドホック分析エージェント - 分析依頼から問い構造化→分析→解釈→追加検証を段階実行"
requires-python = ">=3.11,<3.14"
dependencies = [
    "google-analytics-data>=0.18.0",
    "google-auth>=2.20.0",
]

[tool.uv]
package = false

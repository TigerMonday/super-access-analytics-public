# 集客計測チェックリスト

**対象クライアント**: `{クライアント名}`
**監査日**: `{YYYY-MM-DD}`
**監査者**: パラメータ管理エージェント / レビュアー: `{担当者名}`
**対象期間（GA4データ）**: `{YYYY-MM-DD ～ YYYY-MM-DD}`

---

## サマリ

- **指摘総数**: `{N}` 件（High: `{n}` / Mid: `{n}` / Low: `{n}`）
- **チェック対象媒体数**: `{N}` 媒体
- **欠損流入率**: `{XX.X}%`（`(not set)` + `(direct)` の合計セッション割合）

### 優先対応事項（Top 3）

1. **`{タイトル}`** — `{1行要約}`（重大度: High）
2. **`{タイトル}`** — `{1行要約}`（重大度: High）
3. **`{タイトル}`** — `{1行要約}`（重大度: Mid）

---

## 媒体×項目マトリクス

凡例: ✅ 問題なし / ⚠ 部分的に問題 / ❌ 大きな問題 / — 該当なし

| 媒体 | 目的 | A. utm設定 | B. 命名規則 | C. 欠損流入 | D. 目的整合 | E. CD完備 |
|---|---|:-:|:-:|:-:|:-:|:-:|
| `{媒体1}` | 獲得 | ✅ | ⚠ | ✅ | ✅ | ❌ |
| `{媒体2}` | 認知 | ⚠ | ✅ | ⚠ | ⚠ | — |
| `{媒体3}` | 獲得 | ❌ | — | ❌ | ❌ | ❌ |
| ... | | | | | | |

---

## 重大度別 指摘リスト

### 🔴 High（即時対応推奨）

#### 指摘 H-1: `{指摘タイトル}`
- **対象**: `{媒体名 / 該当utm 等}`
- **現状**: `{現在の問題状況、データ根拠}`
- **影響**: `{これがあると何が困るか}`
- **修正案**: `{具体的な修正手順}`
- **実施担当**: `{媒体運用 / GTM実装 / GA4設定 担当}`

#### 指摘 H-2: `{指摘タイトル}`
...

### 🟡 Mid（次回定例までに対応）

#### 指摘 M-1: `{指摘タイトル}`
...

### 🟢 Low（改善余地、できれば対応）

#### 指摘 L-1: `{指摘タイトル}`
...

---

## 不明・追加確認事項

監査時に判断できなかった項目。媒体運用担当 or クライアントに確認が必要。

- [ ] `{確認事項1}`
- [ ] `{確認事項2}`

---

## 適用した前提

- 命名規則: `{プロジェクト固有 / デフォルト適用}`
- 流入データ期間: `{期間}`
- 監査対象媒体: `{N}` 媒体（クライアント指定 / 全媒体）
- 既存 GTM 情報: `{あり / なし}`
- 既存 CD 情報: `{あり / なし}`

---

## 次回監査推奨時期

`{YYYY-MM-DD}` 頃（新規媒体追加時 or 四半期に1回を推奨）

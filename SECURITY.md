# セキュリティ

## 対応範囲

このリポジトリのコード、手順、サンプル、レポート変換機能に起因する脆弱性を対象とします。
GA4、GTM、AI提供会社など外部サービス自体の問題は各提供元へ報告してください。

## サポート対象

| 対象 | セキュリティ修正 |
|---|---|
| `main` ブランチと最新リリース | 対応します |
| 過去のコミット・旧リリース | 原則として対応しません。最新版で再現するか確認してください |

## 報告方法

認証情報、クライアント名、実URL、GA4プロパティID、実データ、再現用の秘密情報を公開Issueへ貼らないでください。

非公開の連絡先を知っている場合は、リポジトリ管理者へ影響と再現条件を連絡してください。連絡先が分からない場合は、秘密情報を含めず「脆弱性を非公開で報告したい」とForgejoのIssueで伝えてください。管理者が連絡方法を案内します。

管理者は内容を確認し、必要に応じて追加情報、回避策、修正版の公開時期を連絡します。

## 安全な運用

- `outputs/` と `01_context_management/context/` は実データを含むためGit管理外です。
- GA4・GTM・Search Consoleの分析用サービスアカウントには、読み取りに必要な権限だけを付与してください。Googleへのレポート出力には、別の出力用アカウントを使います。
- 外部AIへデータを送る前に [docs/data-handling.md](docs/data-handling.md) に従って承認を得てください。
- 生成HTMLは未信頼のMarkdownを入力し得ます。最新版を使い、生成前後のファイルを信頼できる場所で扱ってください。
- 公開前には `uv run python tools/leak_check.py` を単独で実行し、結果を確認してください。

## 修正と公開

重大な脆弱性は、再現情報を公開する前に修正版を用意します。修正版には影響範囲、回避策、
更新が必要な版を記載します。

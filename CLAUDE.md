# super-access-analytics

最初に `docs/ai-agent-guide.md` と `docs/standard-run-order.md` を読む。受付、セットアップ、各機能の必須条件は共通ガイドが示す正本に従い、このファイルへ複製しない。

利用者は自然文で依頼する。目的に合う機能だけを実行し、市場・顧客理解などGA4を使わない依頼にGA4接続を要求しない。

分析・考察は現在のエージェント自身が行う。Pythonから別のAIを自動起動しない。外部送信は `docs/data-handling.md` に従う。

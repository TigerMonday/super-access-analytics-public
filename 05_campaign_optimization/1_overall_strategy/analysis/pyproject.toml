[project]
name = "overall-strategy"
version = "0.1.0"
description = "全体施策エージェント - GA4データからボトルネックを特定し、CVR改善の全体方針（LPO/コンテンツ/サイト/EFOの着手先＋優先順位）を出す"
requires-python = ">=3.11,<3.14"
dependencies = [
    "google-analytics-data>=0.18.0",
    "google-auth>=2.20.0",
    "pyyaml>=6.0",
]

[tool.uv]
package = false

[project]
name = "target-page-analysis"
version = "0.1.0"
description = "個別施策・分析エージェント - 全体方針のタイプに該当するページ群を分析し、対象ページ決定の根拠を出す"
requires-python = ">=3.11,<3.14"
dependencies = [
    "google-analytics-data>=0.18.0",
    "google-auth>=2.20.0",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
